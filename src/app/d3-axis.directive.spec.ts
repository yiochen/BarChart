import { D3AxisDirective } from './d3-axis.directive';

describe('D3AxisDirective', () => {
  it('should create an instance', () => {
    const directive = new D3AxisDirective();
    expect(directive).toBeTruthy();
  });
});
